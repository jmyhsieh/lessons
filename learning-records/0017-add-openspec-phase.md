# 新增 Claude Code × OpenSpec Phase 13

Status: active

使用者確認 OpenSpec 足以建立獨立 Phase，並要求課程保留與 Phase 3 的直接比較。Phase 13 固定採入門 01–06、進階 07–09：入門在真實 brownfield repo 走完選路、初始化、讀懂 current specs／changes、提出並審查小型變更、受控實作與 archive；進階才逐份產生 artifacts、查核 artifact／code drift，以及管理平行 changes。

Phase 3 仍負責需求追問、triage、除錯、架構診斷、TDD、code review 與 handoff。選擇 OpenSpec 後，該變更以 <code>openspec/changes/&lt;name&gt;/</code> 為規格正本，不再另外維護一份 <code>.scratch</code> spec／tickets。Phase 13 課文完成後需依 <code>speak-human-tw</code> 全文掃描。
